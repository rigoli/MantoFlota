# Resumen de corridas de prueba (E3)

Fecha de referencia: generado al cerrar la implementación E3 en el repositorio.

## Backend

- Comando: `make test-backend` (pytest con cobertura al 100 % sobre `app/`).
- Resultado: **OK** — todos los tests pasan; cobertura total **100 %**.
- Detalle completo de consola: `RUN_BACKEND.txt`.

## Frontend

- Comandos: `pnpm test`, `pnpm lint`, `pnpm typecheck` (desde `frontend/`).
- Resultado: **OK** — Vitest 4 tests en 2 archivos; ESLint y TypeScript sin errores.
- Salida: `RUN_FRONTEND.txt` (última corrida).

## Capturas

Las capturas de pantalla las añade el alumno según la lista en `README.md` de esta carpeta.
